# Build Log — 2026-08-22

## Session: Merge "kkcp updates" drop folder into the repo

**Branch:** `feat/kkcp-updates-merge` (from `main` @ `b9c043f`)
**Commit:** `17df192`

### Context

An untracked folder `kkcp updates/` sat inside the repo root — a full standalone
copy of the project (no `.git` of its own), developed outside version control.
Task: reconcile it into the tracked repo on a new branch.

### Verification before merging

Established that the drop folder is a **successor** to `main`, not a parallel fork:

| Check | Finding |
|---|---|
| Hero rotation (main's Aug-4 work) | Carried forward — same 3 photos (IMG_0286/0336/0348), same 5000ms cadence, reimplemented as `ImageSlider` against `/kkcp/web/` |
| Windows-safe path fix (`b9c043f`) | Intent preserved — `public/images-src/` → `public/images/`, no trailing space |
| `public/images/kkcp/kkcp-logo.png` deletion | Safe — unreferenced; header/footer now use `/kkcp/kkcp_logo_.png` |
| Deleted `.cjs`/`.sh` clone scaffolding | Deliberate — one-off generators, recoverable from history |

### Completed

| Item | Detail |
|---|---|
| Branch created | `feat/kkcp-updates-merge` |
| File reconciliation | 577 added, 597 deleted, 289 modified, 46 renamed (1509 files) |
| New pages | `/syllabus-links`, `/testimonials`, `/tie-up-hospital`, `/scholarships-react` |
| New components | `KkcpHeader/Footer/Title`, `InfoHeader`, `MenuActivator`, `RevealActivator`, `AnimatedCounter`, `TestimonialSlider`, `ImageSlider`, `AdmissionsSection`, `CourseCard`, `lib/faculty.ts` |
| Renames | `CloneTree` → `KkcpTree` (47 pages); `CloneHeader/Footer` → `KkcpHeader/Footer`; `clone-theme.css` → `kkcp-theme.css` |
| Removals | `Univet*` components, clone-scaffolding scripts, legacy `/about-us` `/all-programs` `/faculty-of-science` |
| Assets | jpg/png → webp across all page asset dirs |
| Deps | + `lucide-react`, `react-icons` |
| Tree parity | `diff -rq` vs drop folder: identical except gitignored `next-env.d.ts` |
| Deploy | Vercel **preview** — https://test-dummy-kkcp-lew2f7okp-yogeshthangms-projects.vercel.app |

### Quality gates

| Gate | Result |
|---|---|
| `tsc --noEmit` | ✅ exit 0 |
| `next build` | ✅ exit 0 — compiled in 4.7s, 53 static pages |
| `eslint` | ⚠️ 66 errors / 166 warnings — **down from 92 errors** on `main` baseline (pre-existing debt, see ERR-001) |
| Route smoke test | ✅ 9/9 routes 200 with correct titles; `/nonexistent-xyz` correctly 404s |

### Cleanup

| Item | Detail |
|---|---|
| `kkcp updates/` drop folder | **Deleted** (user-approved). Verified byte-identical to commit `17df192` first — only gitignored build artifacts (`next-env.d.ts`, `tsconfig.tsbuildinfo`) differed. Recoverable via `git checkout 17df192 -- <path>`. Declined to commit it: ~98MB of exact duplicates would have doubled `.git` (114M → ~210M) permanently, undoable only by history rewrite. |
| Secret hygiene | `.vercel/` and `.env.local` (holds `VERCEL_OIDC_TOKEN`) confirmed covered by `.gitignore` lines 37 and 34 — never staged. |
| Post-deletion re-verify | `tsc --noEmit` exit 0; `next build` exit 0, 53 static pages |
| Branch pushed | `origin/feat/kkcp-updates-merge` — working tree clean |

### In progress

None.

### Next

| Item | Blocked on |
|---|---|
| Merge `feat/kkcp-updates-merge` → `main` | **User green signal** (explicitly requested gate) |
| Promote preview → production | User approval; `vercel deploy --prod` |
| Source the missing `optimize-images.cjs` | Referenced by `.gitignore` for regenerating `public/kkcp/web/` derivatives, but absent from the drop folder |

---

## Session 2: Merge `kkcp-updates-2/` drop folder

**Branch:** `feat/kkcp-updates-2` (from `origin/feat/kkcp-updates-merge` @ `86d5621`)
**Commit:** `3756069`

### Base selection

Branched from `feat/kkcp-updates-merge`, **not** `main`. The drop folder was
diffed against both: it is an incremental delta on top of session 1's merge
(14 files, no deletions, strict superset), not a fresh full drop. Branching from
`main` would have re-done the 1509-file merge and destroyed the reviewable
separation between the two drops.

Note: `main` is still at `b9c043f`. This branch therefore carries **both** drops.

### Changes — feature/fix (8)

| File | Change |
|---|---|
| `HeroSection.tsx` (new, 116L) | Responsive hero `h-150` → `2xl:h-240`, replaces fixed 960px Slider Revolution block |
| `CampusCarousel.tsx` (new, 288L) | Breakpoint-aware carousel (1/2/3 slides), cloned slides for seamless loop |
| `test-dummy-webs-1 KkcpTree` | Wires both in; old blocks commented out, not deleted |
| `ImageSlider.tsx` | **Real bug:** `duration-1800` is not a Tailwind class → transition never applied. Now `duration-[1800ms]`. Plus `object-center`. |
| `KkcpFooter.tsx` | External link gets `target="_blank"` + `rel="noopener noreferrer"` |
| `MessageSlider.tsx` | Drop `max-width:80%` on message body |
| 4× `kkcp-theme.css` | Banner art → `/images/kkcp/kkcp_clg1.jpeg`, position `center` |
| `kkcp_clg1.jpeg` (new) | 197KB banner photo |

### Changes — reformat only (3)

Verified non-semantic by comparing element, `href`, `src`, `className`, `data-*`
and inline-style counts — all identical:

- `test-dummy-webs-2 KkcpTree` — the +9849/-56 line diff is prettier reformatting
  of a previously-minified file, plus `data-settings` switching from escaped
  double quotes (`{"{\"a\":1}"}`) to single-quoted JSX (`{'{"a":1}'}`). Same JSON.
- `campus KkcpTree`, `research KkcpTree` — prettier reformatting.

### Quality gates

| Gate | Result |
|---|---|
| `tsc --noEmit` | ✅ exit 0 |
| `next build` | ✅ exit 0 — 53 static pages |
| `eslint` | ⚠️ 67 errors vs **66** on base branch — one new, see ERR-003 |

### Deploy

| Item | Detail |
|---|---|
| Preview | `dpl_Fc1nAdSrdSMgysKHx4bmMDnuMrsJ` — verified, then superseded |
| **Production** | `dpl_HEaNXXAKgE9WK58o7cVmcY1hcSeq` → https://test-dummy-kkcp.vercel.app — exit 0 |
| Verified live | `2xl:h-240` ×1, `sm:h-162.5` ×1, `duration-[1800ms]` ×3, `translate-x-[-12%]` ×2, `rel="noopener noreferrer"` ×1; old `960px` hero 12 → **0** |
| Routes | 9/9 · 200 with correct titles; `/nonexistent-xyz` → 404 (negative control) |
| Assets | `kkcp_clg1.jpeg` serves 201468 B, matches local byte-for-byte |

Both drops are therefore live in production while `main` is still at `b9c043f`.
The deployed artifact is built from `feat/kkcp-updates-2`, not from `main`.

### Known defect carried into production

**CRLF line endings on ~354 committed files** (see ERR-005). No effect on the
rendered site — the route smoke tests above are the evidence — but every future
diff, blame and merge on those files will be noisy until it is normalised.

### Next

| Item | Blocked on |
|---|---|
| Merge to `main` | **User green signal** (carries both drops) |
| Normalise line endings (ERR-005) | User go-ahead — needs a force-push to the branch |
| Preserve the 36 commits on `origin/content/kkcp-md-verbatim` | User decision — rebase vs keep the squash |
| `public/images/kkcp/kkcp_clg.jpeg` now unreferenced | Left tracked — deletion not requested (scope discipline) |
